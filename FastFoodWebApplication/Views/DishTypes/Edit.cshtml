@model FastFoodWebApplication.Models.DishType

@{
    ViewData["Title"] = "Edit";
}
@await Html.PartialAsync("_HeadPartial")

<h1 style ="margin-left: 100px">Edit</h1>
<h4 style="margin-left: 100px">DishType</h4>
<hr />
<div class="row" style="margin-left: 100px">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" style="margin-left: 100px">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
